# 2026-04-18 16:28 - 整理 GPT-SoVITS-V2 指南为永久文档

## 一、本次会话目标

把上一轮 Ask 模式中口头给出的 "GPT-SoVITS-V2 部署 / 训练 / 对接" 指南整理为仓库内的永久文档，并在 `README.md` 关键位置补上链接。

## 二、操作清单

| 序号 | 操作 | 文件 |
| --- | --- | --- |
| 1 | 新建完整指南文档（9 个章节，含训练流程 / API 启动 / OLV 对接 / 常见坑表） | [docs/sovits_setup.md](../sovits_setup.md) |
| 2 | 在 `README.md` 的 "5. 启动外部服务" 段落上方加一行引导，指向新指南 | [README.md](../../README.md) |
| 3 | 在 `README.md` 末尾新增 "专题文档" 区块，集中收录 sovits_setup / polish_checklist / AI完成内容 三个入口 | [README.md](../../README.md) |
| 4 | 写本次会话记录（即本文件）到 `docs/AI完成内容/` | 本文件 |
| 5 | git add + commit + push 到 `origin/main` | - |

## 三、新增文档章节结构

`docs/sovits_setup.md` 共 9 节：

1. GPT-SoVITS-V2 是什么（GPT 模型 + SoVITS 模型职责拆解）
2. 环境准备（Windows 推荐方式 + 路径建议）
3. 训练自己的音色（语料准备 + WebUI 四个 Tab 详解）
4. 启动 API v2 服务（含启动参数、健康检查、端点速查、`/tts` 关键参数）
5. 对接到 AICompanion / OLV（`conf.yaml` 三个必改字段 + 注意点）
6. 推荐的启动顺序（mermaid 流程图 + start-all.ps1 用法）
7. 常见坑（8 项症状-原因-解决对照表）
8. 官方资料链接
9. 与项目的关联文件

## 四、设计要点

- **不重复内容**：训练流程的细节集中在 sovits_setup.md，README 只引用而不复述
- **路径修正**：sovits_setup.md 在 docs/ 下，引用根目录文件用 `../` 前缀
- **可发现性**：README 末尾新增 "专题文档" 区块，未来加入新指南只需追加一行链接
- **会话记录归档**：按用户约定的 `时间_执行操作.md` 命名规范写入 `docs/AI完成内容/`

## 五、提交信息

待执行：

```
docs: add GPT-SoVITS-V2 setup guide and link from README
```
